# v1.1.2 — 기기 모델 변경 없이 MuMu 인스턴스 확인

MuMu 6.4.7.0에서 Samsung·Asus 모델로 표시되는 실제 인스턴스를 v1.1.1이 지원 대상에서 제외하던 현장 문제를 수정했습니다. 모델·브랜드 설정을 변경할 필요가 없습니다.

- MuMuManager의 실시간 실행 상태·로컬 ADB 주소와 동일 설치 폴더의 실제 인스턴스 PID를 교차 확인합니다.
- 검사와 권한 변경·복원 직전에 호스트 정보를 다시 확인합니다. 저장된 주소, 정적 설정, 원격 주소, 중지된 VM, 다른 프로그램의 PID는 신원 근거로 사용하지 않습니다.
- 공식 ADB 주소로 점검하여 동일 VM의 별칭을 중복 집계하지 않습니다. 해당 VM 연결 실패는 성공으로 숨기지 않습니다.
- 기존 Store 검사, 원래 권한/설치 식별 백업, 최소 권한 예약 작업, 설치 사본, 원복 절차는 유지합니다. 관리자 권한 ADB에 접근할 수 없으면 기존처럼 변경을 보류합니다.

네 브랜드의 모의 차단·백업·복원과 잘못된/사라진 호스트 증거 거부를 검사합니다. Windows CI는 v1.1.1에서 갱신, 설치 사본 실행, 테스트 원본 삭제 이후 실제 1분 반복 2회와 해제를 검증합니다. 사용자 PC에서는 모델 변경 없이 두 인스턴스와 Store 12.1.12의 읽기 전용 인식에 성공했습니다.

지원 범위는 com.mumu.store의 오버레이입니다. 홈 추천·게임 내부·Windows 광고는 포함하지 않습니다. 두 인스턴스 모두 적용 전부터 중앙 팝업이 보이지 않았으므로, 권한 검사 성공을 광고 발생 전후 비교 완료로 표현하지 않습니다. 실제 PC 적용 기록은 릴리스 설명의 후속 검증에 따로 남깁니다.
